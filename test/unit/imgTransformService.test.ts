import { imageTransformService } from '../../src/services/imgTransformService';

describe('ImageTransformService', () => {

  afterEach(() => {
    jest.clearAllMocks();
  });

  describe('compressImage', () => {
    it('should compress the image buffer', async () => {
      // test for image compression
    });
  });

  describe('rotateImage', () => {
    it('should rotate the image buffer', async () => {
      // test for image rotation
    });
  });

  describe('createThumbnail', () => {
    it('should create a thumbnail from the image buffer', async () => {
      // test for image thumbnail creation
    });
  });

  // more tests to follow for further transformations
});
